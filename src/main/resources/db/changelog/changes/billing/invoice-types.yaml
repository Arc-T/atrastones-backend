databaseChangeLog:
  - changeSet:
      id: 1767965250459-1
      author: Mr.Arc-T
      changes:
        - createTable:
            schemaName: billing
            tableName: invoice_types
            remarks: Stores invoice type definitions
            columns:
              - column:
                  name: id
                  type: ${id.type}
                  autoIncrement: true
                  constraints:
                    nullable: false

              - column:
                  name: name
                  type: ${name.type}
                  constraints:
                    nullable: false

              - column:
                  name: description
                  type: ${short_text.type}
                  constraints:
                    nullable: true

              - column:
                  name: created_at
                  type: ${timestamp_value.type}
                  defaultValueComputed: CURRENT_TIMESTAMP
                  constraints:
                    nullable: false

              - column:
                  name: updated_at
                  type: ${timestamp_value.type}
                  constraints:
                    nullable: true

        - addPrimaryKey:
            schemaName: billing
            tableName: invoice_types
            columnNames: id
            constraintName: pk_invoice_types

        - addUniqueConstraint:
            schemaName: billing
            tableName: invoice_types
            columnNames: name
            constraintName: uk_invoice_types_name