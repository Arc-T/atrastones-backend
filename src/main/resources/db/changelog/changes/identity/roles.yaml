databaseChangeLog:
  - changeSet:
      id: 1767965009054-1
      author: Mr.Arc-T
      changes:
        - createTable:
            schemaName: identity
            tableName: roles
            remarks: Stores user roles for RBAC
            columns:
              - column:
                  name: id
                  type: ${id.type}
                  autoIncrement: true
                  constraints:
                    nullable: false

              - column:
                  name: name
                  type: ${name.type}
                  constraints:
                    nullable: false

              - column:
                  name: description
                  type: ${short_text.type}
                  remarks: Role description for clarity
                  constraints:
                    nullable: true

              - column:
                  name: created_at
                  type: ${timestamp_value.type}
                  defaultValueComputed: CURRENT_TIMESTAMP
                  constraints:
                    nullable: false

              - column:
                  name: updated_at
                  type: ${timestamp_value.type}
                  constraints:
                    nullable: true

        - addPrimaryKey:
            schemaName: identity
            tableName: roles
            columnNames: id
            constraintName: pk_roles

        - addUniqueConstraint:
            schemaName: identity
            tableName: roles
            columnNames: name
            constraintName: uk_roles_name
