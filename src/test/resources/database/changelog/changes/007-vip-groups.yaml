databaseChangeLog:
  - changeSet:
      id: 7
      author: taha
      changes:
        - createTable:
            tableName: vip_groups
            comment: Stores VIP group definitions
            columns:
              - column:
                  name: id
                  type: ${id.type}
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: name
                  type: ${name.type}
                  constraints:
                    nullable: false
              - column:
                  name: description
                  type: ${short_text.type}
                  constraints:
                    nullable: true
              - column:
                  name: created_at
                  type: ${timestamp_value.type}
                  defaultValueComputed: CURRENT_TIMESTAMP
                  constraints:
                    nullable: false
              - column:
                  name: updated_at
                  type: ${timestamp_value.type}
                  constraints:
                    nullable: true
        - addUniqueConstraint:
            tableName: vip_groups
            columnNames: name
            constraintName: uk_vip_groups_name